"""Interactive image canvas: view, label overlay and edit tools.

Import from the submodules (``tda.ui.canvas.view``, ``.overlay``, ``.tools``)
rather than from this package: ``view`` doubles as the manual entry point
``python -m tda.ui.canvas.view``, and re-exporting it here would make ``runpy``
import the module twice.
"""
