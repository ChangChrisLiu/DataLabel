"""Qt (PySide6) user interface for the Teardown Annotator.

Everything that imports Qt lives under ``tda.ui``; ``tda.core`` stays Qt-free
so the compiler, checks and exporters can run headless (see spec 10.1).
"""
